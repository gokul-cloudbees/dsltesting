apiVersion: cloudbees.com/v2025.12
kind: property
metadata:
  name: ec_usageCount
  description: A count of how many times this Environment Template has been used to
    spin up Environments
spec:
  value: "{% cb_include_as_yaml_comment '../ec_usageCount.txt' %}"
  suppressValueTracking: true
  propertyType: string
